<% if current_user.borrow?(@book) %>
    <%= form_tag(book_path) do %>
    <%= hidden_field_tag :user_id, current_user.id %>
    <%= submit_tag '返却', class: 'btn btn-success' %>
    <% end %>
<% else %>
    <%= form_tag(book_path) do %>
    <%= hidden_field_tag :user_id, current_user.id %>
    <%= submit_tag '貸出', class: 'btn btn-primary' %>
    <% end %>
<% end %>